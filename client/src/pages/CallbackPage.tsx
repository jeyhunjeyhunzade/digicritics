import Layout from "@app/layout/AppLayout";

const CallBackPage = () => {
  return (
    <Layout>
      <div className="flex items-center justify-center">Callback Page</div>
    </Layout>
  );
};

export default CallBackPage;

export enum Status {
  ACTIVE = "ACTIVE",
  BLOCKED = "BLOCKED",
}

export enum Languages {
  EN = "en",
  RU = "ru",
}

export enum ProfileTableCategories {
  REVIEWNAME = "",
}

export enum UserStatus {
  USER = "User",
  ADMIN = "Admin",
  BLOCKED = "Blocked",
}
